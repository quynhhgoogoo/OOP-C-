
//this program defines a struct for student information, which includes the following fields:
//name (an array of char), id (unsigned int) and grade (unsigned int). Define an array of 5 locations
// which includes student information and print out student information sorted in aescending order
// according to student grade.
#include <iostream>
#include <cstring>
using namespace std;

struct Students s[5]
{
   char name[50];
   unsigned int id;
   unsigned int grade; 
};
Students tempt;

int main( )
{
   struct Students A;        // Declare the first student
   struct Students B;        // Declare the second student
   struct Students C;
   struct Students D;
   struct Students E;
 
   // first student information
   strcpy( A.name, "Elsa");
   A.id = 1000;
   A.grade = 5;

   //second student information
   strcpy( B.name, "Eiei");
   B.id = 2000;
   B.grade = 4;
   
   strcpy( C.name, "Tom");
   C.id = 3000;
   C.grade = 3;
   
   strcpy( D.name, "Mei");
   D.id = 4000;
   D.grade = 2;
   
   strcpy( E.name, "Tayo");
   E.id = 5000;
   E.grade = 1;
   
    int k, j;
        for(k=0; k<5; k++){
                for(j=0; j<5; j++){
                        if(Students[k].grade<Students[j].grade){
                                tempt=Students[k].grade;
                                Students[k].grade=Students[j].grade;
                                Students.grade[j]=tempt;

}
                }
        }
        for(k=0; k<5; k++)      cout<<Students[k].name<<' '<<Students[k].id<<' '<<Students[k].grade<<endl;
}
